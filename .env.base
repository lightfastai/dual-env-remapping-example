# Base environment variables shared across all contexts and services
# These can be overridden per-context or per-service using `dual env set`

DATABASE_URL=postgres://localhost/default_db
REDIS_URL=redis://localhost:6379/0
DEBUG=false
LOG_LEVEL=info
API_KEY=default-api-key
NODE_ENV=development
